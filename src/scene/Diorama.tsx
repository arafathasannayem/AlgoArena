/**
 * Diorama — Main React Three Fiber canvas with isometric camera.
 *
 * ## TODO — Phase 3
 *
 * - Set up R3F `<Canvas>` with an orthographic camera at true isometric angle
 *   (~35.264° tilt, 45° rotation).
 * - Render Tile grid, Wall volumes, GoalGlow.
 * - One directional "sun" light + soft ambient fill + shadow-mapping.
 * - No orbit controls unless explicitly requested.
 *
 * @module scene/Diorama
 */

export function Diorama() {
  // TODO: Phase 3 — implement isometric diorama scene
  return null;
}
